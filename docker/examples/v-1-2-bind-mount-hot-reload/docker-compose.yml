# 예제 V-1-2: bind mount 핫 리로드
# 학습 시나리오: main.py 수정 → uvicorn 자동 재시작 → curl로 확인
# 단일 진실원: ../../docs/design/crossref.md §3

services:
  web:
    build: .
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./app:/app
    ports:
      - "8000:8000"
